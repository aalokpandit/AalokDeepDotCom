# Main-site environment variables for local development
# In production, these are set via Azure Static Web Apps configuration

# Headshot image URL
# Local development: uses /public/images/AalokPanditHeadshot.png (default if unset)
# Production: set to blob storage URL via SWA environment config
# NEXT_PUBLIC_HEADSHOT_URL=https://<storageaccount>.blob.core.windows.net/main-site/headshot.png
